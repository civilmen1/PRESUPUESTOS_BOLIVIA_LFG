# --- Núcleo / UI ---
streamlit>=1.30
pandas>=2.0
python-dotenv>=1.0

# --- Lectura de tablas Excel ---
openpyxl>=3.1
xlrd>=2.0            # soporte .xls heredado

# --- Lectura de documentos técnicos ---
pypdf>=4.0
python-docx>=1.1

# --- OCR para DBCs escaneados / imágenes (opcional) ---
# Requiere instalar el binario Tesseract OCR y Poppler en el sistema.
# Windows: https://github.com/UB-Mannheim/tesseract/wiki  y  poppler para Windows.
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0

# --- Scraping web (Nivel 2) ---
requests>=2.31
beautifulsoup4>=4.12

# --- Extracción con IA / LLM ---
# Gemini (nivel gratuito) para generar recursos con IA. Se activa con GEMINI_API_KEY.
google-genai>=1.0
# Opcionales (de pago), se activan solo si configuras su API key:
# openai>=1.0           # GPT-4o (extracción estructurada de partidas)
# anthropic>=0.40       # Claude Sonnet (interpretación normativa NB)

# --- Verificación de SEPREC ---
# La verificacion usa la API oficial (servicios.seprec.gob.bo) con requests.
# Playwright es OPCIONAL, solo si activas SEPREC_USAR_NAVEGADOR como respaldo:
# playwright>=1.40

# --- Exportación PDF ---
reportlab>=4.0

# --- Pruebas ---
pytest>=8.0

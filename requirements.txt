# --- Núcleo / UI ---
streamlit>=1.30
pandas>=2.0
python-dotenv>=1.0

# --- Lectura de tablas Excel ---
openpyxl>=3.1
xlrd>=2.0            # soporte .xls heredado

# --- Lectura de documentos técnicos ---
pypdf>=4.0
python-docx>=1.1

# --- OCR para DBCs escaneados / imágenes (opcional) ---
# Requiere instalar el binario Tesseract OCR y Poppler en el sistema.
# Windows: https://github.com/UB-Mannheim/tesseract/wiki  y  poppler para Windows.
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0

# --- Scraping web (Nivel 2) ---
requests>=2.31
beautifulsoup4>=4.12

# --- Extracción con IA / LLM (opcional, multi-modelo) ---
# Se activan solo si configuras la API key correspondiente en .env.
# openai>=1.0           # GPT-4o (extracción estructurada de partidas)
# anthropic>=0.40       # Claude Sonnet (interpretación normativa NB)
# google-generativeai>=0.8  # Gemini (análisis multimodal de planos PDF)

# --- Verificación de SEPREC automatizando el portal (opcional) ---
# Requiere ademas: playwright install chromium
playwright>=1.40

# --- Exportación PDF ---
reportlab>=4.0

# --- Pruebas ---
pytest>=8.0
